# AutoTriage-ITSM — root environment template
# docker compose reads this file for ${VAR} interpolation.

# Optional: enabling either switches Tier-2 classification + LLM RAG steps on.
OPENAI_API_KEY=
GOOGLE_GEMINI_API_KEY=
